development:
  adapter: postgresql
  url: <%= ENV.fetch("DATABASE_URL") { "postgresql://localhost:5432/thingybase_development" } %>

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: thingybase_production
