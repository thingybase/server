= simple_form_for resource do |f|
  = f.input :name, autofocus: true
  = f.association :parent, label: "Contained in", collection: container_select_values(account_policy_scope(Container)), include_blank: true
  = f.association :account, as: :hidden
  = f.button :submit, class: 'is-primary'
