%h1.title Copy #{@item.name}

= simple_form_for @item_copy do |f|
  = f.input :number_of_copies, autofocus: true, as: :integer
  = f.input :item_id, as: :hidden
  = f.button :submit, class: 'is-primary', value: "Create copies"
