= title resource.name, subtitle: breadcrumb(item_breadcrumbs), icon: @item.icon

.notification
  %p.mb-4 Item created
  .field= link_to "Copy", new_item_copy_path(@item), class: 'button is-fullwidth'
  .field= link_to "Change icon", edit_item_icon_path(@item), class: 'button is-fullwidth'
  .field= link_to "View label", label_path(resource.label), class: 'button is-fullwidth'
