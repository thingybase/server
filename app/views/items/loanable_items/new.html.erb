<%= title @item.name, subtitle: item_breadcrumb(@item, leaf: "Loan"), icon: @item.icon %>

<%= application_form @loanable_item, url: url_for(action: :create) do |f| %>
  <%= f.button :submit, class: 'btn btn-primary', value: "Add to borrowing list" %>
<% end %>