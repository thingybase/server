p
  #{@member_request.user.name}, with the email address #{@member_request.user.email}, has requested to join the "#{@member_request.account.name}" Thingybase account that you own.

p
  | You may accept or decline this request at #{link_to new_member_request_review_url(@member_request), new_member_request_review_url(@member_request)}.
