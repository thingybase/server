= simple_form_for @notification, :html => { :class => 'form-horizontal' } do |f|
  = f.association :team, collection: policy_scope(Team), include_blank: false
  = f.input :subject, autofocus: true
  = f.input :message, as: :text, input_html: { rows: 7 }
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), :back, :class => 'btn btn-default'
