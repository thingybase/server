%section.hero.is-fullheight-with-navbar.is-bold
  .hero-body
    .container
      = title "You do not have permissions to #{@permission} this #{@forbidden_resource.model_name.human.downcase}", icon: "no-entry-badge"
      %p
        You're logged in and acccessing this resource as #{link_to current_user.email, current_user}.
