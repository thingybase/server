.page-header
  %h1=t '.title', :default => resource_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= resource_class.human_attribute_name(:name)
  %tbody
    - @accounts.each do |account|
      %tr
        %td= link_to account.name, account_path(account)

= link_to t('.new', :default => t("helpers.links.new")), new_account_path, :class => 'btn btn-primary'
