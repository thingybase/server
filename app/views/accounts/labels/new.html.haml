%h1.title New label
= simple_form_for @label, :html => { :class => 'form-horizontal' } do |f|
  = f.input :text, autofocus: true, hint: "hi"
  = f.association :account, as: :hidden
  = f.button :submit, :class => 'is-primary'