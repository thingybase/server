%h1.title API Keys
%h2.subtitle Connect to other applications and services with API keys

= link_to "+ New API key ", new_api_key_path, class: 'button is-primary'

.list-card
  - @api_keys.each do |api_key|
    .list-card--item
      .list-card--item--title= link_to api_key.name, api_key_path(api_key)
      .list-card--item--details
        .list-card--item--detail
          Created
          =local_time api_key.created_at
